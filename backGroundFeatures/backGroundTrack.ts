import DiscordJs from 'discord.js'
import timePlayed from '../models/timePlayed';
export async function backgroundTrack() {
    setInterval(() => {//wait 6 hours before we track players in the background, and repeat it 
        /* TODO:
            1. get all Players Id and the
        */
    }, 1000 * 60 * 60 * 3)
}